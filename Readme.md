
Following are the useful commands

Build and run tests :
 mvn install
 
Run App : 
 mvn spring-boot:run

Application runs on 9000 port. 

All REST APIs are available at http://localhost:9000/swagger-ui.html#/
